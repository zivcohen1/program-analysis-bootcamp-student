// Demonstrates while loops.
fun sum_to(n) {
  total = 0;
  i = 1;
  while (i <= n) {
    total = total + i;
    i = i + 1;
  }
  return total;
}

fun countdown(start) {
  i = start;
  while (i > 0) {
    print(i);
    i = i - 1;
  }
  print(0);
  return;
}
