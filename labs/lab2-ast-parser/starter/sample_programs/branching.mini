// Demonstrates if-else branching.
fun abs(n) {
  if (n < 0) {
    result = 0 - n;
  } else {
    result = n;
  }
  return result;
}

fun classify(x) {
  if (x > 0) {
    label = 1;
  } else {
    if (x == 0) {
      label = 0;
    } else {
      label = -1;
    }
  }
  return label;
}
